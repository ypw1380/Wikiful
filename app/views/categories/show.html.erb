<h1>This is the show view for Categories</h1>

<div>
  <h1><%= @category.name %>Articles</h1>
  <h2>There are <%= @category.articles.count %> articles for <%= @category.name %></h2>
  
  <ul>
   <% @category.articles.order(:title).each do |article| %>
 		<h4>
 	  	<%= link_to article.title, article %>
 	  </h4>
 	 <% end %>
  </ul>
</div>
 